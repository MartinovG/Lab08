Gabriel
pass1234

DevOps Overview
🛠️ DevOps & Cloud Engineering Plan – Staging NestJS in AWS

👤 Assigned to: Gabriel Martinov


🧭 Цели на проекта:

* Контейнеризация на NestJS приложението
* Изграждане на инфраструктура в AWS с Terraform
* Деплой в ECS + мониторинг с CloudWatch
* CI/CD pipeline с GitHub Actions


🔧 Етап 1: Локална подготовка


✅ Инсталации:

* Docker Desktop + Docker CLI - check
* WSL2 (ако е на Windows) - check
* Terraform CLI (под WSL) - check
* AWS CLI (под WSL) - check
* GitHub CLI (по избор) - check


✅ Свързване с AWS:

* Конфигурирай aws configure с IAM credentials - kalata
* Тествай достъпа до AWS (aws sts get-caller-identity) - kalata


🐳 Етап 2: Контейнеризация на приложението


✅ Docker:

* Напиши Dockerfile за NestJS - check
* Добави .dockerignore - check
* Тествай локално с docker build и docker run -check


🧩 Ако има база данни:

* Създай docker-compose.yml за приложение + DB - mai check


☁️ Етап 3: Terraform – AWS инфраструктура


🌐 VPC + мрежа:

* S3 backend + state locking за Terraform
* VPC с CIDR блок
* Public и private subnets
* Internet Gateway + NAT Gateway
* Route Tables
* Security Groups


🚀 ECS & ECR:

* Създай ECR repository за контейнерите
* ECS Cluster (EC2 vs Fargate?)
* Task Definition + Service
* Application Load Balancer
* Route53 record(s)
* ACM


📊 Етап 4: Monitoring & Observability

📈 CloudWatch:

* Лог групи за ECS
* Аларми:
    * CPU utilization > 80%
    * Memory usage > 75%
    * Task health check failures


🔁 Етап 5: CI/CD с GitHub Actions

✅ Pipeline стъпки:

* Build + Test NestJS приложението
* Docker build и push в ECR
* Използване на OpenID Connect (OIDC) за оторизране на GitHub в AWS


⚙️ Усложнение (по избор):

* Deployment на среди (dev, staging, prod)
* Strategy: rolling update / blue-green


🎓 Бонус задачи (по избор):

* Custom CloudWatch dashboard
* SNS известия при аларми
* Health checks with Route53
* Secrets management (AWS SSM или Secrets Manager)


📁 Помощни ресурси:

* AWS Docs: VPC, ECS, Terraform AWS provider
* GitHub Actions: Docker + AWS ECR Example


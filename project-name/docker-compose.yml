version: '3.8'  

services:
  app:
    build: .
    environment:
      - DB_HOST=nestjs-postgres.cbso24u0kz31.eu-north-1.rds.amazonaws.com
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=postgres07
      - DB_NAME=mydb
    ports:
      - '3000:3000'
